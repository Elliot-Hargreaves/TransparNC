//! Core VPN state types and transitions.

/// Placeholder for application core state until implementation arrives.
///
/// Following the project's guideline, we'll represent major states
/// as enums to avoid invalid combinations.
#[allow(dead_code)]
pub(crate) enum CoreState {
    /// Initial state before any network setup.
    Init,
}
